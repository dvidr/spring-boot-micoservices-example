# spring-boot-micoservices-example

spring boot applications can be started with 

```./mvnw spring-boot:run```

Angular client can be started from in the client directory

```npm install```

```npm start```
